//6.var arr = [1,1,2,2,3,4,5,4,3,2,1], 打印出数字2出现的次数。
var arr = [1,1,2,2,3,4,5,4,3,2,1]//定义一个数组，进行数据储存
var num=0//声明一个变量用于求2出现次数
for (var i = 0; i < arr.length; i++)//循环遍历score数组
{
    if(arr[i]==2)//如果数字为2
    {
        num++//记2出现的次数
    }
}
console.log(num)//打印输出
